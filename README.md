# Marketing-Analytics
This script fetches the Bank Marketing dataset from the UCI repository and applies a supervised learning pipeline to predict term deposit subscription. It preprocesses data using one-hot encoding and class-balanced Logistic Regression, with performance evaluated using precision, recall, F1-score, and ROC-AUC for imbalanced data.
